import { StyleSheet } from 'react-native';

export const LoginTheme = StyleSheet.create({
    title: {
        color: 'white',
        fontSize: 30,
        fontWeight: 'bold',
    },
    label: {
        marginTop: 25,
        color: 'white',
        fontWeight: 'bold',
    },
});
